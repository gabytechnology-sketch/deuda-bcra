[build]
  publish = "."                  # publica desde la raíz del repo (donde está index.html)
  command = ""                   # no hace falta build step (es HTML + JS estático + functions)
  functions = "netlify/functions"  # apunta EXACTAMENTE a tu carpeta

# Opcional: fuerza rebuild de functions
[functions]
  directory = "netlify/functions"
